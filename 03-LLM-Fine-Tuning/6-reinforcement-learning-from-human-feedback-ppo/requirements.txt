# Core stack
torch
transformers
datasets
accelerate

# RLHF / PPO trainer
trl>=0.11.0

# PEFT / LoRA (recommended for PPO — three models in VRAM)
peft

# Quantization (4-bit / 8-bit via --load-in-4bit / --load-in-8bit)
bitsandbytes

# Optional integrations
wandb
huggingface_hub
sentencepiece
protobuf
